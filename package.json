{
  "name": "highlight-note-api",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for Hugo highlight note feature",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
